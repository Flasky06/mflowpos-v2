{
  "name": "mflowpos-v2",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:frontend",
    "build:frontend": "cd mflow-v2-frontend && npm install && npm run build",
    "build:backend": "cd mflow-v2-backend && npm install && npm run build"
  }
}
